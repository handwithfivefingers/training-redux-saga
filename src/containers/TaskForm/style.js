const style = (theme) => ({
  TextField: {
    width: '100%',
  },
  content: {
    padding: theme.spacing(2),
  },
  select: {
    display: 'flex',
    flexWrap: 'wrap',
  },
});
export default style;
